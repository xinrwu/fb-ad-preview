'use strict';

describe('previewApp.preview_list module', function() {

  beforeEach(module('previewApp.preview_list'));

  describe('preview list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PreviewListController = $controller('PreviewListController');
      expect(PreviewListController).toBeDefined();
    }));

  });
});
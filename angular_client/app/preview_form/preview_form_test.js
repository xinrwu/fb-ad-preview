'use strict';

describe('previewApp.preview_form module', function() {

  beforeEach(module('previewApp.preview_form'));

  describe('preview form controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PreviewFormController = $controller('PreviewFormController');
      expect(PreviewFormController).toBeDefined();
    }));

  });
});